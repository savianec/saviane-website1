export const metadata = {
  title: "Portal",
  description: "saviane client portal",
};

export default function AppRootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
